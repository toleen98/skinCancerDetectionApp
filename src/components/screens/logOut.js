import React from "react";

import App from "../../../App"
export default class Logout extends React.Component {
    componentDidMount() {

    }
    render() {
        return (
            <App independent={true} />

        )
    }
}